-- Database dedicated to functional tests (the default target of tests/functional).
-- Run automatically by the postgres container on first start.
-- The schema is applied by the tests themselves (beforeAll).
CREATE DATABASE stayup_test;
